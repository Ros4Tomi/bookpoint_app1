server:
  port: 8086

spring:
  application:
    name: ms-pagos
  datasource:
    url: jdbc:h2:mem:pagosdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true