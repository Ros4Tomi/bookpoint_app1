server:
  port: 8088

spring:
  application:
    name: ms-resenas
  datasource:
    url: jdbc:h2:mem:resenasdb;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true